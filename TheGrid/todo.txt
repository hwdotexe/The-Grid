Add level up notifications
Make items configurable (name, block ID, etc.)
+/spawn command
+Fix traceroute animation
+Traceroute should give the name of the system
+if a player is cooling down & disconnects, just save config as true. When they join again, start a 45s timer to cooldown again.

Add a way to remove systems via GUI or command. Need a way to list systems.